{
  "name": "@demlik/example-checkout-saga",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Durable order/checkout saga in a Cloudflare Durable Object, with Cmd handlers authored as Effect programs via @demlik/tea/effect.",
  "scripts": {
    "dev": "wrangler dev --port 8790",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
