/**
 * @packageDocumentation
 * @demlik/tea/effect — the Effect engine's entry point, empty until the engine
 * lands. It is published now so the import-graph guard covers it from the start.
 *
 * `effect` is an optional peer dependency, and this entry is the only place in
 * the package allowed to import it.
 */

export {};
