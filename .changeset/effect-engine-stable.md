---
"@demlik/tea": minor
---

`@demlik/tea/effect` and `@demlik/tea/testing/effect` graduate from `experimental` to `stable`. The Effect engine now runs Tuval fully, so it carries the stable semver promise: a breaking change needs a major bump and a migration note. No code changes.
